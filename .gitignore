# 옛 보관 자리 — 2026-09-13 에 기본값이 저장소 밖으로 나갔다. 새로 시작하면 이 폴더들은
# 생기지 않지만, 그전부터 쓰던 기계에는 남아 있고 서버가 그 자리를 계속 쓴다.
video-clip/
projects/
models/
.clipserver.json

tmp-verify/
.DS_Store

# 파이썬이 server.py 를 돌릴 때마다 만든다 — 추적하면 실행할 때마다 트리가 더러워진다.
__pycache__/
*.pyc
